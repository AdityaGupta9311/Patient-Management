version: '3.8'

services:
  billing-service:
    container_name: billing-service
    build:
      context: .
      dockerfile: Dockerfile
    image: billing-service:latest
    ports:
      - "8001:8001"
      - "9001:9001"
    networks:
      - internal

networks:
  internal:
    external: true

